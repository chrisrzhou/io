import README from "../../README.md";
import Layout from "layouts/Layout";

export default function AboutPage({ children }) {
  return (
    <Layout description="About me" title="About">
      {children}
    </Layout>
  );
}

<README />
